{
  "name": "offers",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ toledoroy/offers",
    "create-local": "graph create --node http://localhost:8020/ toledoroy/offers",
    "remove-local": "graph remove --node http://localhost:8020/ toledoroy/offers",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 toledoroy/offers"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
